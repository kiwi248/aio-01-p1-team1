# Codex 작업 인수인계서

마지막 확인일: 2026-08-06 (Asia/Seoul)

## 1. 문서의 목적

이 문서는 다른 컴퓨터에서 Codex를 다시 실행했을 때 `FE_User`의 로그인 및 회원가입 화면 개발을 바로 이어가기 위한 인수인계 문서다.

사용자는 초보 프로그래머이므로 다음 방식으로 협업해야 한다.

- 어려운 용어를 줄이고 12살도 이해할 수 있는 수준으로 설명한다.
- 코드를 바로 수정하기 전에 먼저 사용자에게 복사할 코드를 보여준다.
- 어느 파일의 어느 부분을 바꾸는지 정확히 알려준다.
- 한 번에 너무 많은 파일을 수정하지 않는다.
- 기존 코드 스타일과 구조를 최대한 유지한다.
- 사용자가 붙여넣은 뒤 실행 화면이나 오류를 확인하면서 단계별로 진행한다.

## 2. 프로젝트 정보

- 프로젝트: 공공임대 및 분양 청약 통합 안내 서비스
- 프런트엔드: Streamlit
- 인증: Supabase Auth
- 작업 대상: `FE_User`
- 로그인 페이지: `FE_User/app_pages/00_login.py`
- 회원가입 페이지: `FE_User/app_pages/01_signup.py`
- 페이지 등록 및 내비게이션: `FE_User/app.py`
- 인증 함수: `FE_User/core/auth.py`
- Supabase 연결: `FE_User/core/supabase_client.py`

로그인과 회원가입은 이미 별도의 Streamlit 페이지로 등록되어 있다. 따라서 회원가입 버튼을 누르면 `01_signup.py`로 이동하게 만들면 되고, 새로운 페이지 파일을 추가할 필요는 없다.

## 3. 사용자의 최종 요구사항

### 로그인 페이지

- 로그인 버튼 옆에 회원가입 버튼을 배치한다.
- 회원가입 버튼을 누르면 회원가입 페이지로 이동한다.
- 이메일을 아이디 부분과 도메인 부분으로 나눈다.
- 도메인은 셀렉트박스에서 선택한다.
- 현재 결정된 도메인 목록:
  - `naver.com`
  - `gmail.com`
  - `daum.net`
  - `kakao.com`
- 사용자는 `직접 입력` 항목과 별도의 직접 입력 칸이 필요 없다고 결정했다.
- 로그인 요청 직전에 `아이디@도메인` 형태로 합친다.

### 회원가입 페이지

- 로그인과 동일한 이메일 도메인 셀렉트박스를 사용한다.
- 회원가입 버튼을 누르면 별도 회원가입 페이지가 나타난다.
- 참고 영상과 비슷한 약관 동의 화면을 만든다.
- 전체 동의 체크박스를 제공한다.
- 회원가입 약관 필수 체크박스를 제공한다.
- 개인정보 수집 및 이용 필수 체크박스를 제공한다.
- 필수 약관에 동의하지 않으면 회원가입을 막고 알기 쉬운 오류 메시지를 표시한다.
- 약관 아래에 이메일, 비밀번호, 비밀번호 확인, 닉네임 입력 폼을 둔다.
- 기존 `sign_up(email, password, nickname)` 함수는 가능한 한 그대로 재사용한다.

## 4. 참고 영상에서 확인한 화면

원래 참고 영상의 로컬 경로는 다음과 같았다.

```text
/Users/son/Desktop/화면 기록 2026-08-06 오후 10.26.08.mov
```

다른 컴퓨터에는 이 파일이 없을 수 있으므로 필요하면 사용자에게 다시 첨부해 달라고 요청한다.

영상에서 확인한 구성은 다음과 같다.

1. `서비스 이용약관에 동의해 주세요` 제목
2. 필수 약관에 동의해야 회원가입이 가능하다는 안내
3. 가입 단계 표시
4. `약관 전체 동의` 체크박스
5. `필수 · 회원가입 약관` 영역
6. `회원가입 약관에 동의합니다` 체크박스
7. `필수 · 개인정보 수집 및 이용` 영역
8. 목적, 수집 항목, 보유 기간을 보여주는 표
9. `개인정보 수집 및 이용에 동의합니다` 체크박스
10. 하단의 취소 및 회원가입 버튼
11. 약관 미동의 상태에서 진행하면 가입을 제한하는 안내

영상의 상표, 로고, SNS 가입 버튼을 그대로 복제하는 것이 목표는 아니다. 현재 Streamlit 프로젝트 스타일 안에서 약관 구조와 동작을 구현하는 것이 목표다.

## 5. 현재 실제 저장 상태

### `FE_User/app_pages/00_login.py`

로그인 화면은 일부 수정되어 있다.

현재 구현된 내용:

- 이메일 아이디 입력 칸
- 이메일 도메인 셀렉트박스
- 로그인 버튼과 회원가입 페이지 링크를 2열로 배치
- 로그인 전에 이메일 아이디와 도메인을 결합
- 빈 값 및 `@` 중복 입력 검사

하지만 다음 문제가 남아 있다.

```python
EMAIL_DOMAINS = [
    "naver.com",
    "gmail.com",
    "daum.net",
    "kakao.com",
    "직접 입력",
]
```

사용자가 `직접 입력`을 제거하기로 했으므로 목록의 마지막 항목을 삭제해야 한다.

또한 현재 파일 38~42행 부근에 다음 코드가 남아 있다.

```python
if selected_domain == "직접 입력":
    email_domain = email_domain = selected_domain
else:
    email_domain = selected_domain
```

이 부분은 직접 입력 기능을 제거하는 과정에서 잘못 남은 코드다. 다음 한 줄로 바꿔야 한다.

```python
email_domain = selected_domain
```

### `FE_User/app_pages/01_signup.py`

이 파일은 아직 원래 코드 그대로다. 약관 체크박스와 이메일 도메인 셀렉트박스가 아직 구현되지 않았다.

현재 제공하는 입력값은 다음 네 가지뿐이다.

- 이메일
- 비밀번호
- 비밀번호 확인
- 닉네임

다음 작업에서 기존 검증과 `sign_up()` 호출은 유지하면서 약관 UI와 이메일 분리 입력을 추가해야 한다.

### 그 밖의 파일

- `FE_User/app.py`는 수정하지 않았다.
- `FE_User/core/auth.py`는 수정하지 않았다.
- `FE_User/core/supabase_client.py`는 수정하지 않았다.
- 새 회원가입 페이지를 만들 필요가 없다. `01_signup.py`가 이미 등록되어 있다.

## 6. 다음 Codex가 진행할 권장 순서

### 1단계: 로그인 코드 정리

사용자에게 먼저 아래 두 가지 변경을 설명하고 코드를 보여준다.

1. `EMAIL_DOMAINS`에서 `"직접 입력"` 삭제
2. 잘못 남은 `if/else` 전체를 `email_domain = selected_domain` 한 줄로 교체

사용자가 복사하여 붙여넣은 다음 로그인 페이지가 정상 표시되는지 확인한다.

### 2단계: 회원가입 이메일 입력 개선

`01_signup.py`에도 로그인 페이지와 같은 `EMAIL_DOMAINS` 목록을 추가한다. 이메일 아이디 입력 칸과 도메인 셀렉트박스를 2열로 표시하고 제출 시 다음처럼 합친다.

```python
full_email = f"{clean_email_id}@{selected_domain}"
```

이메일 아이디 칸에 사용자가 `@`를 입력하면 `@ 앞부분만 입력해 주세요.`라고 안내한다.

### 3단계: 약관 UI 구현

회원가입 정보 폼 위에 다음 UI를 추가한다.

- 페이지 제목과 안내 문구
- 진행 단계 또는 `st.progress`
- 전체 동의 체크박스
- `st.expander`를 이용한 회원가입 약관
- `st.expander`를 이용한 개인정보 수집 및 이용 표
- 각 필수 동의 체크박스

중요: Streamlit의 `st.form` 안에서는 체크박스 변경 결과가 제출 전까지 즉시 반영되지 않을 수 있다. 전체 동의가 즉시 개별 체크박스에 반영되어야 한다면 약관 체크박스는 가입 정보 `st.form` 바깥에 두고 `st.session_state`와 `on_change`를 사용하는 방식을 우선 검토한다.

### 4단계: 회원가입 검증

회원가입 버튼을 눌렀을 때 다음 순서로 검사한다.

1. 필수 약관 두 개에 동의했는지
2. 이메일 아이디가 비어 있지 않은지
3. 이메일 아이디에 `@`가 포함되지 않았는지
4. 비밀번호가 비어 있지 않은지
5. 비밀번호가 최소 6자인지
6. 비밀번호와 확인 값이 일치하는지
7. 닉네임이 비어 있지 않은지
8. 모두 통과하면 기존 `sign_up(full_email, password, nickname)` 호출

### 5단계: 동작 확인

다음 명령은 `FE_User` 폴더에서 실행한다.

```bash
source .venv/bin/activate
streamlit run app.py
```

이미 실행 중이라면 `Ctrl+C`로 종료한 뒤 다시 실행한다.

확인 항목:

- 로그인 페이지에 도메인 셀렉트박스가 보이는가
- `직접 입력` 항목이 완전히 사라졌는가
- 로그인 버튼 옆에 회원가입 버튼이 보이는가
- 회원가입 버튼을 누르면 회원가입 페이지로 이동하는가
- 전체 동의가 필수 체크박스에 반영되는가
- 하나라도 미동의하면 가입이 차단되는가
- 이메일이 `아이디@선택한도메인`으로 만들어지는가
- 비밀번호 불일치 안내가 작동하는가
- 정상 정보로 Supabase 회원가입 요청이 되는가

## 7. 환경 설정 및 실행 주의사항

`FE_User/.env` 파일에 다음 환경변수가 필요하다.

```env
SUPABASE_URL=https://실제프로젝트ID.supabase.co
SUPABASE_ANON_KEY=실제-anon-key
```

주의사항:

- 실제 URL이나 키를 이 인수인계 문서에 기록하지 않는다.
- 프런트엔드에는 Service Role Key를 넣지 않는다.
- `.env`는 Git에 커밋하지 않는다.
- 새 컴퓨터에서는 `.env.example`을 복사한 다음 실제 값을 입력한다.

```bash
cd FE_User
cp .env.example .env
```

macOS 가상환경 활성화 명령:

```bash
source .venv/bin/activate
```

필요 패키지 설치:

```bash
pip install -r requirements.txt
```

Conda의 `(base)`가 새 터미널마다 나타나면 다음 명령으로 자동 활성화를 끌 수 있다.

```bash
conda config --set auto_activate_base false
```

## 8. Git 상태 관련 메모

마지막 확인 당시 Git 상태는 다음과 같았다.

```text
 M FE_User/app_pages/00_login.py
?? .vscode/
```

- `00_login.py` 변경은 사용자가 직접 붙여넣은 작업이므로 보존한다.
- `.vscode/`는 사용자 개인 설정일 수 있으므로 임의로 삭제하거나 덮어쓰지 않는다.
- 작업 시작 전 반드시 `git status --short`로 최신 상태를 다시 확인한다.
- 사용자의 기존 변경을 `git reset`, `git checkout --` 등으로 되돌리지 않는다.

## 9. 현재 범위에서 하지 말아야 할 것

- 사용자 확인 없이 프로젝트 전체 디자인을 바꾸지 않는다.
- `app.py`의 내비게이션 구조를 불필요하게 재작성하지 않는다.
- Supabase 인증 구조를 새 라이브러리로 교체하지 않는다.
- `.env`의 비밀값을 출력하거나 문서에 복사하지 않는다.
- 관리자용 `FE_Admin` 파일을 이번 작업에 섞지 않는다.
- 사용자 코드의 다른 변경을 임의로 되돌리지 않는다.

## 10. 다음 대화 시작 시 권장 안내

다음 Codex는 먼저 실제 파일을 읽고 이 문서의 상태와 일치하는지 확인한다. 그다음 사용자에게 다음과 같이 짧게 안내하는 것이 좋다.

> 인수인계 내용을 확인했습니다. 먼저 로그인 페이지에 남아 있는 `직접 입력` 항목과 잘못된 조건문을 정리한 뒤 실행을 확인하겠습니다. 그다음 회원가입 페이지의 약관과 입력 폼을 한 단계씩 추가하겠습니다. 먼저 복사할 코드를 보여드릴게요.

이후 사용자의 “코드를 먼저 보고 직접 복사해 붙여넣기” 방식을 계속 따른다.
